import * as React from 'react';

import './NotFound.scss';

const NotFound: React.FC = () => {

    return (
        <div className="not-found">
            <h1 className="app__title">Page Not Found</h1>
        </div>
    );
};

export default NotFound;
